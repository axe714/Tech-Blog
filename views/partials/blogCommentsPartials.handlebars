<div class='comment-container'>
  <div class='comment-header'>
    <h2>{{{user.username}}} responded on {{{comment_date}}}:</h2>
  </div>
  <div class='comment-content-container'>
    <h3 class='comment-content-text'>{{{comment_content}}}</h3>
  </div>
</div>