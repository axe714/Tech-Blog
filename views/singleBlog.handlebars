{{#if logged_in}}
<div class='single-blog-main-container'>
  <div class='single-blog-container'>
    <div class='single-blog-title-header'>
      <h1>{{{blog.blog_title}}} - Written by {{{blog.user.username}}}</h1>
    </div>
    <div class='single-blog-content'>
      <h3>{{{blog.blog_content}}}</h3>
    </div>
    <div class='single-blog-footer'>
      <button class="single-blog-comment-btn">Add a comment</button>
    </div>
  </div>
  <div class='comment-main-container'>
        {{#each blog.comments }}
        {{> blogCommentsPartials}}
        {{/each}}
  </div>
</div>
{{/if}}

<script src="/js/singleBlog.js"></script>